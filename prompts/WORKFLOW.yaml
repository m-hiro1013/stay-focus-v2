# =====================================
# 💾 WORKFLOW.yaml
# =====================================
# プロジェクトの完全なセーブデータ
#
# 【更新ルール】
# 🔴 毎セッション更新 → session_state のみ
# 🟢 機能追加時のみ → features, feature_details
# 🟡 たまに更新 → decisions, cautions, file_structure, changelog
#
# =====================================

workflow:
  version: "3.0"
  last_updated: "2025-12-30"

  # =====================================
  # 🔴 SESSION_STATE（毎回ここだけ更新）
  # =====================================
  session_state:
    last_summary: |
      F017 ピン留め・重要マークの表示ルール変更を実装完了：
      - 並び順をsort_orderのみに変更（ピン・重要は影響しない）
      - ピン留め: 赤枠線(2px) + bold + 📌アイコン
      - 重要マーク: 薄黄色背景 + ⭐アイコン（期日切れで上書き）
      - プロジェクトカラー: 左ボーダー → タスク名横のカラー丸に変更

    next_tasks:
      - "F053: Service Worker実装（低優先度）"

    pending_specs: []
    # 要件定義で作成した変更仕様書はここに追加
    # 形式:
    # - id: CS-001
    #   title: "〇〇機能追加"
    #   priority: high
    #   created_at: "2025-12-30"
    #   background: "..."
    #   related_features: [F010]
    #   targets:
    #     - file: src/main.js
    #       function: xxx
    #       current: "..."
    #       new: "..."
    #   implementation:
    #     approach: "..."
    #     code_hints: [...]
    #   considerations: [...]
    #   acceptance_criteria: [...]
    #   confirmed_decisions: [...]

    completed_specs: []
    # 実装完了した変更仕様書の履歴

  # =====================================
  # 🟢 FEATURES（機能追加時のみ更新）
  # =====================================
  # status: done / in_progress / needs_fix / not_started / on_hold
  features:
    # --- 認証 ---
    F001: { name: "ログイン・サインアップ・ログアウト", status: done, page: auth.html }
    F002: { name: "チーム自動作成（トリガー）", status: done, note: "Supabaseトリガー" }
    F003: { name: "マルチユーザー対応（RLS）", status: done, note: "20ポリシー設定済み" }

    # --- タスク管理 ---
    F010: { name: "タスク一覧表示（時間枠グループ）", status: done, page: index.html }
    F011: { name: "タスク作成", status: done, page: index.html }
    F012: { name: "タスク編集（詳細モーダル）", status: done, page: index.html }
    F013: { name: "タスク削除", status: done, page: index.html }
    F014: { name: "タスク完了・Undo", status: done, page: index.html, note: "Cmd+Z/Ctrl+Z対応" }
    F015: { name: "D&D並び替え（同一時間枠）", status: done, page: index.html }
    F016: { name: "D&D時間枠移動", status: done, page: index.html }
    F017: { name: "ピン・重要マークの表示ルール", status: done, page: index.html }
    F018: { name: "期日切れ・時間枠不一致警告", status: done, page: index.html }

    # --- プロジェクト管理 ---
    F020: { name: "プロジェクトタブ表示", status: done, page: index.html }
    F021: { name: "プロジェクト作成", status: done, page: index.html }
    F022: { name: "プロジェクト編集・削除", status: done, page: index.html }
    F023: { name: "プロジェクト完了", status: done, page: index.html }
    F024: { name: "アーカイブ・復元", status: done, page: index.html }

    # --- メンバー管理 ---
    F030: { name: "メンバー追加・削除", status: done, page: index.html }
    F031: { name: "担当者設定・色表示", status: done, page: index.html }
    F032: { name: "メンバー編集", status: done, page: index.html, note: "Vanilla JS版で独自追加" }

    # --- レポート ---
    F040: { name: "期間指定レポート", status: done, page: report.html }
    F041: { name: "振り返りコメント編集", status: done, page: report.html }

    # --- PWA ---
    F050: { name: "PWAモード判定", status: done }
    F051: { name: "スワイプ操作（完了・削除）", status: done, note: "PWAモード時のみ" }
    F052: { name: "オフライン検出・表示", status: done }
    F053: { name: "Service Worker", status: not_started, note: "低優先度" }

    # --- 追加機能 ---
    F060: { name: "削除Undo", status: done, page: index.html }

  # =====================================
  # 🟢 FEATURE_DETAILS（詳細が必要な機能のみ）
  # =====================================
  feature_details:
    F001:
      description: |
        メール/パスワードでの認証機能。
        ログイン後はindex.htmlにリダイレクト。
        ログアウト後はauth.htmlにリダイレクト。

    F002:
      description: |
        サインアップ時にSupabaseトリガーが発火し、
        teamsテーブルにレコード作成、
        team_membersにownerとして追加される。

    F010:
      description: |
        タスクを時間枠（今日/明日/今週/来週/来月以降）ごとにグループ化して表示。
        各グループ内はsort_order順でソート。
        完了済みタスク（is_completed=true）は非表示。

    F011:
      description: |
        タスク名を入力してEnterで即座に作成。
        作成モーダルで詳細（期日・時間・メモ・担当者）も設定可能。
        デフォルトの時間枠は「今日」。

    F014:
      description: |
        チェックボックスクリックで完了/未完了を切り替え。
        完了時はタスクが一覧から消え、Undoトースト表示（5秒間）。
        Cmd+Z / Ctrl+Z またはトーストのUndoボタンで取り消し可能。

    F017:
      description: |
        ピン留め・重要マークは並び順に影響せず、見た目のみ変更。
        【ピン留め】赤枠線(2px solid #ff4d4d) + bold + 📌アイコン
        【重要マーク】薄黄色背景(#fffde7) + ⭐アイコン（期日切れで赤背景上書き）
        【両方ON】赤枠線 + 薄黄色背景 + bold + 両アイコン
        【プロジェクトカラー】タスク名の前にカラー丸(12px)で表示

    F018:
      description: |
        期日が過去、または時間枠と矛盾している場合に警告表示。
        赤背景・警告アイコンで視覚的に強調。
        判定: 期日 < 今日 → 期日切れ / 期日 < 時間枠の最小日数 → 不一致

    F024:
      description: |
        完了済みプロジェクトをアーカイブ可能。
        アーカイブモーダルで一覧表示、復元操作。
        クエリ条件: is_archived=true のみ（is_completedは問わない）

    F031:
      description: |
        タスク作成・編集時に担当者を複数選択可能。
        タスクカードに担当者のカラーバッジを表示。
        assigneesカラム（JSONB配列）にメンバーIDを保存。

    F051:
      description: |
        右スワイプ: タスク完了
        左スワイプ: タスク削除
        PC版（ブラウザ）では無効、PWAモード時のみ有効。

    F060:
      description: |
        タスク削除後にUndoトースト表示（5秒間）。
        Cmd+Z / Ctrl+Z またはUndoボタンで復元。
        復元時はidを除外して新規INSERT（id重複エラー回避）。

  # =====================================
  # 🟡 DECISIONS（たまに追加）
  # =====================================
  decisions:
    adopted:
      D001:
        idea: "フロントエンドはVanilla JS + Vite（React不使用）"
        reason: "シンプルさ優先、原価管理アプリと同じ構成"
      D002:
        idea: "CSSはTailwindCSS v4を使わず、各HTMLファイル内にstyleタグ"
        reason: "TailwindCSS v4の設定が複雑、シンプルに保つ"
      D003:
        idea: "assigneesはJSONB配列で保存"
        reason: "柔軟性重視、中間テーブルより簡単"
      D004:
        idea: "priority_time_frameは5種類固定（今日/明日/今週/来週/来月以降）"
        reason: "既存仕様を維持、シンプルさ優先"
      D005:
        idea: "タスクのソートはsort_orderのみ（ピン・重要は並び順に影響しない）"
        reason: "手動並び替えを優先、ユーザーの意図を尊重"
      D006:
        idea: "プロジェクトカラーはタスク名横のカラー丸で表示（左ボーダー廃止）"
        reason: "ピン留めの赤枠線と競合するため"

    rejected:
      R001:
        idea: "状態管理ライブラリ（Redux等）の導入"
        reason: "オーバーエンジニアリング、Vanilla JSで十分"
      R002:
        idea: "ピン留め・重要マークで自動的に先頭に移動"
        reason: "手動並び替えを優先、ユーザーの意図を尊重"
      R003:
        idea: "TypeScriptの導入"
        reason: "シンプルさ優先、学習コスト削減"

  # =====================================
  # 🟡 CAUTIONS（バグ修正時に追加）
  # =====================================
  cautions:
    C001:
      issue: "RLSでデータが取れない"
      symptom: "データが空配列で返ってくる"
      solution: ".eq('team_id', currentTeamId) を追加"
      example: |
        // NG
        const { data } = await supabase.from('tasks').select('*');
        // OK
        const { data } = await supabase.from('tasks').select('*').eq('team_id', currentTeamId);

    C002:
      issue: "モーダル背景クリックで閉じない"
      symptom: "モーダル内のクリックでも閉じてしまう"
      solution: "e.stopPropagation() をモーダルコンテンツ側に追加"
      example: |
        modal.addEventListener('click', () => closeModal());
        modalContent.addEventListener('click', (e) => e.stopPropagation());

    C003:
      issue: "Undo後にid重複エラー"
      symptom: "復元時にduplicate key errorが発生"
      solution: "idを除外して新規INSERTする"
      example: |
        const { id, ...taskWithoutId } = deletedTask;
        await supabase.from('tasks').insert(taskWithoutId);

    C004:
      issue: "完了タスクのカウントが不正"
      symptom: "完了数が実際より多い/少ない"
      solution: "filter後のlengthを使う"
      example: |
        // NG
        const count = tasks.length;
        // OK
        const count = tasks.filter(t => t.is_completed).length;

    C005:
      issue: "ドラッグがスマホで動かない"
      symptom: "タッチデバイスでD&Dが効かない"
      solution: "touchイベント併用 or PWAのみスワイプ"

    C006:
      issue: "プロジェクト選択がリセットされる"
      symptom: "タスク操作後に「すべて」タブに戻る"
      solution: "再描画後にactiveTabIdを維持する"

    C007:
      issue: "settingsテーブルのINSERTエラー"
      symptom: "idカラムがないためエラー"
      solution: "settingsはteam_id + keyの複合PKなのでidは不要"

  # =====================================
  # 🟡 FILE_STRUCTURE（ファイル追加時のみ更新）
  # =====================================
  file_structure:
    root:
      - index.html      # メイン画面
      - auth.html       # 認証画面
      - report.html     # レポート画面
      - vite.config.js  # Vite設定
      - package.json    # 依存関係
      - .env            # 環境変数
    src:
      - main.js          # メイン画面ロジック
      - auth.js          # 認証画面ロジック
      - report.js        # レポート画面ロジック
      - supabaseClient.js # Supabase初期化
      - pwaDetector.js   # PWA判定
    public:
      - manifest.json    # PWAマニフェスト
      - sw.js            # Service Worker（未実装）
      - icons/           # PWAアイコン

  # =====================================
  # 🟡 CHANGELOG（実装完了時に追記）
  # =====================================
  changelog:
    - date: "2025-12-XX"
      changes:
        - "初期6テーブル作成（GAS版からの移行）"
        - "RLS有効化・20ポリシー追加"
        - "サインアップ時のチーム自動作成トリガー追加"

    - date: "2025-12-30"
      changes:
        - "F017 ピン留め・重要マークの表示ルール変更"
        - "並び順をsort_orderのみに変更"
        - "プロジェクトカラー: 左ボーダー → カラー丸に変更"
        - "実DBとの差分を修正（settings複合PK、due_time型等）"